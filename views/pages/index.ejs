<!-- Hero Section -->
<section class="py-16 md:py-24 bg-gradient-to-b from-primary-50 to-white  ">
  <div class="container mx-auto px-4 ">
    <div class="flex flex-col md:flex-row items-center">
      <div class="md:w-1/2 mb-10 md:mb-0" data-aos="fade-right">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-800 mb-6">
          Magical <span class="text-primary-500">Products</span> for Your Kids
        </h1>
        <p class="text-lg text-gray-600 mb-8">
          Discover our collection of high-quality, safe, and fun products designed especially for children of all ages.
        </p>
        <div class="flex space-x-4">
          <a href="/products" class="btn-primary">
            Shop Now
            <i class="fas fa-arrow-right ml-2"></i>
          </a>
          <a href="/about" class="btn-outline">
            Learn More
          </a>
        </div>
      </div>
      
    
<div class="w-full  md:w-1/2 relative  " data-aos="fade-left">
  <div class="relative h-[250px] sm:h-[300px] md:h-[500px] w-full ">
    <!-- Product Carousel -->
    <div class="product-carousel h-full w-full relative">
      <% if (featuredProducts.length > 0) { %>
        <% featuredProducts.forEach(function(product, index) { %>
          <div class="carousel-item   inset-0 flex items-center justify-center transition-opacity duration-500 ease-in-out <%= index === 0 ? 'opacity-100' : 'opacity-0' %>">
            <img 
              src="<%= product.image %>" 
              alt="<%= product.name %>" 
              class="h-[180px] sm:h-[220px] md:h-[400px] object-contain w-auto mx-auto"
            loading="lazy" >
          </div>
        <% }); %>
      <% } else { %>
        <div class="carousel-item absolute inset-0 flex items-center justify-center animate-float">
          <div class="text-center">
            <p class="text-lg text-gray-500">No featured products available</p>
          </div>
        </div>
      <% } %>
    </div>

    <!-- Carousel Controls -->
    <% if (featuredProducts.length > 1) { %>
      <button class="carousel-prev absolute top-1/2 -left-4 transform -translate-y-1/2 bg-white shadow-md rounded-full p-3 hover:bg-gray-100 z-10">
        <i class="fas fa-chevron-left text-primary-500"></i>
      </button>
      <button class="carousel-next absolute top-1/2 -right-4  transform -translate-y-1/2 bg-white shadow-md rounded-full p-3 hover:bg-gray-100 z-10">
        <i class="fas fa-chevron-right text-primary-500"></i>
      </button>
    <% } %>
  </div>
</div>

   </div>
  </div>
</section>

<!-- Categories Section -->
<section class="py-16 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">Explore Categories</h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Find the perfect products for your children across our diverse categories.
      </p>
    </div>
    
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-6" data-aos="fade-up" data-aos-delay="200">
      <a href="/products?category=toys" class="category-card flex flex-col items-center p-6 rounded-2xl bg-primary-50 hover:bg-primary-100 transition-colors text-center">
        <div class="w-16 h-16 flex items-center justify-center mb-4 rounded-full bg-primary-100">
          <i class="fas fa-puzzle-piece text-2xl text-primary-500"></i>
        </div>
        <h3 class="font-medium text-gray-800">Toys</h3>
      </a>
      
      <a href="/products?category=clothes" class="category-card flex flex-col items-center p-6 rounded-2xl bg-secondary-50 hover:bg-secondary-100 transition-colors text-center">
        <div class="w-16 h-16 flex items-center justify-center mb-4 rounded-full bg-secondary-100">
          <i class="fas fa-tshirt text-2xl text-secondary-500"></i>
        </div>
        <h3 class="font-medium text-gray-800">Clothes</h3>
      </a>
      
      <a href="/products?category=books" class="category-card flex flex-col items-center p-6 rounded-2xl bg-accent-50 hover:bg-accent-100 transition-colors text-center">
        <div class="w-16 h-16 flex items-center justify-center mb-4 rounded-full bg-accent-100">
          <i class="fas fa-book text-2xl text-accent-500"></i>
        </div>
        <h3 class="font-medium text-gray-800">Books</h3>
      </a>
      
      <a href="/products?category=accessories" class="category-card flex flex-col items-center p-6 rounded-2xl bg-green-50 hover:bg-green-100 transition-colors text-center">
        <div class="w-16 h-16 flex items-center justify-center mb-4 rounded-full bg-green-100">
          <i class="fas fa-socks text-2xl text-green-500"></i>
        </div>
        <h3 class="font-medium text-gray-800">Accessories</h3>
      </a>
      
      <a href="/products?category=games" class="category-card flex flex-col items-center p-6 rounded-2xl bg-purple-50 hover:bg-purple-100 transition-colors text-center">
        <div class="w-16 h-16 flex items-center justify-center mb-4 rounded-full bg-purple-100">
          <i class="fas fa-gamepad text-2xl text-purple-500"></i>
        </div>
        <h3 class="font-medium text-gray-800">Games</h3>
      </a>
      
      <a href="/products?category=art" class="category-card flex flex-col items-center p-6 rounded-2xl bg-yellow-50 hover:bg-yellow-100 transition-colors text-center">
        <div class="w-16 h-16 flex items-center justify-center mb-4 rounded-full bg-yellow-100">
          <i class="fas fa-paint-brush text-2xl text-yellow-500"></i>
        </div>
        <h3 class="font-medium text-gray-800">Art</h3>
      </a>
    </div>
  </div>
</section>

<!-- Featured Products Section -->
<section class="py-16 bg-gray-50">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">Featured Products</h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Check out our most popular and loved products for kids.
      </p>
    </div>
    
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
      <% if (featuredProducts.length > 0) { %>
        <% featuredProducts.slice(0, 8).forEach(function(product) { %>
          <div class="product-card card" data-aos="fade-up" data-aos-delay="<%= Math.floor(Math.random() * 400) %>">
            <div class="relative overflow-hidden h-60">
              <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-full object-contain p-4" loading="lazy">
              <div class="add-to-cart absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center opacity-0 transition-opacity duration-300">
                <a href="/products/<%= product._id %>" class="btn-secondary text-sm">
                  View Details
                </a>
              </div>
            </div>
            <div class="p-4">
              <h3 class="font-medium text-lg mb-1"><%= product.name %></h3>
              <p class="text-gray-500 text-sm mb-2"><%= product.category.charAt(0).toUpperCase() + product.category.slice(1) %></p>
              <div class="flex justify-between items-center">
                <span class="font-bold text-primary-600">$<%= product.price.toFixed(2) %></span>
                <button 
                  class="whatsapp-btn text-green-500 hover:text-green-600"
                  data-name="<%= product.name %>"
                  data-price="<%= product.price.toFixed(2) %>"
                  data-whatsapp="<%= contactInfo.whatsapp %>">
                  <i class="fab fa-whatsapp text-lg"></i>
                </button>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <div class="col-span-full text-center py-10">
          <p class="text-lg text-gray-500">No featured products available at the moment.</p>
          <a href="/products" class="btn-primary mt-4 inline-block">View All Products</a>
        </div>
      <% } %>
    </div>
    
    <div class="text-center mt-16" data-aos="fade-up">
      <a href="/products" class="btn-outline">
        View All Products
        <i class="fas fa-arrow-right ml-2"></i>
      </a>
    </div>
  </div>
</section>

<!-- Why Choose Us Section -->
<section class="py-16 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">Why Choose Us</h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        We offer the best shopping experience for kids' products.
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="text-center p-6" data-aos="fade-up" data-aos-delay="100">
        <div class="w-16 h-16 rounded-full bg-primary-100 flex items-center justify-center mx-auto mb-4">
          <i class="fas fa-shield-alt text-2xl text-primary-500"></i>
        </div>
        <h3 class="text-xl font-bold mb-2">Safe & Quality Products</h3>
        <p class="text-gray-600">All our products undergo strict quality checks to ensure they are safe for children.</p>
      </div>
      
      <div class="text-center p-6" data-aos="fade-up" data-aos-delay="200">
        <div class="w-16 h-16 rounded-full bg-secondary-100 flex items-center justify-center mx-auto mb-4">
          <i class="fas fa-truck text-2xl text-secondary-500"></i>
        </div>
        <h3 class="text-xl font-bold mb-2">Fast Delivery</h3>
        <p class="text-gray-600">We ensure quick and reliable delivery so your kids can enjoy their new items sooner.</p>
      </div>
      
      <div class="text-center p-6" data-aos="fade-up" data-aos-delay="300">
        <div class="w-16 h-16 rounded-full bg-accent-100 flex items-center justify-center mx-auto mb-4">
          <i class="fas fa-undo text-2xl text-accent-500"></i>
        </div>
        <h3 class="text-xl font-bold mb-2">Easy Returns</h3>
        <p class="text-gray-600">Not satisfied? Our hassle-free return policy ensures your peace of mind.</p>
      </div>
    </div>
  </div>
</section>

<!-- Newsletter Section -->
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const items = document.querySelectorAll('.carousel-item');
    const prevBtn = document.querySelector('.carousel-prev');
    const nextBtn = document.querySelector('.carousel-next');
    let currentIndex = 0;

    function showSlide(index) {
      items.forEach((item, i) => {
        item.classList.toggle('opacity-100', i === index);
        item.classList.toggle('opacity-0', i !== index);
      });
    }

    if (prevBtn && nextBtn) {
      prevBtn.addEventListener('click', () => {
        currentIndex = (currentIndex - 1 + items.length) % items.length;
        showSlide(currentIndex);
      });

      nextBtn.addEventListener('click', () => {
        currentIndex = (currentIndex + 1) % items.length;
        showSlide(currentIndex);
      });
    }

    showSlide(currentIndex);
  });
</script>